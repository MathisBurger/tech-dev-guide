mod dsa;

fn main() {
    // Hashmap example
    dsa::hash_maps::hash_map();
    // Hashtable example
    dsa::hash_tables::hash_table();
    // Linked list example
    dsa::linked_list::linked_lists();
}
